#!/usr/bin/env node
var path = require('path');

require('babel-register')({
  "presets": ["es2015"]
});
require('app-module-path').addPath(path.join(__dirname, '../src'));
var commands = require('commands').get();
require('dispatcher').default(commands); 

